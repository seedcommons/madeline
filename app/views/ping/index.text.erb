<% content_for(:title){ "Ping" } -%>
Madeline
App is running.
Background jobs are <%= @sidekiq_running ? "" : "NOT " %>running.
There are <%= @count %> jobs in the queue.
There <%= @stuck ? "ARE" : "are no" %> stuck jobs.
Everything is <%= @ok ? "OK" : "NOT OK" %>.
