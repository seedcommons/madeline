    div.step-logs
      div.icon-with-text.view-logs-ctrl
        span.text-left-of-icon
          - if step.logs_count > 0
            span.show-log
              = t("log.show_logs", n: step.logs_count)
          - else
            span.no-logs
              = t("log.no_logs")
        span.icon-box.add-log
          i.fa.fa-plus-circle

      - if step.logs_count > 0
        section.logs
          header.row
            h1.col-lg-8.first-col Logs
            div.col-lg-4.last-col.action-items
              button.btn New Log

          - step.project_logs.each do |log|
            = render "admin/logs/log", step: step, log: log

