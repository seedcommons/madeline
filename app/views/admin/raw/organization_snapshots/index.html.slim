/ note, this top-level list view isn't really useful, except for migration debugging

= render_index_header

= resolved_render 'organization_snapshots', 'list', {items: @items}

= will_paginate @items