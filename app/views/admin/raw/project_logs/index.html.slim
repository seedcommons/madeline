/ note, this top-level list view isn't really useful, except for migration debugging

= render view_path('common/index_header')

= render view_path('project_logs/list'), {items: @items}

= will_paginate @items