li = link_to t('menu.dashboard'), root_path
li = link_to t('menu.coops'), admin_organizations_path
li = link_to t('menu.loans'), admin_loans_path
li = link_to t('menu.calendar'), admin_calendar_index_path
- if policy(Person).index?
  li = link_to t('people.shared.people'), admin_people_path
- if policy(Division).index?
  li = link_to t('menu.divisions'), admin_divisions_path
