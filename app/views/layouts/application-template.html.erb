<!doctype html>
<html lang="en-US">

  <head>
    <meta charset="UTF-8">
    <title><%= yield :template %> |  The Working World</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield :head %>
  </head>

  <body>
    <section>
      <h1><%= yield :title %></h1>
      <%= yield :content %>
    </section>
  </body>

</html>
