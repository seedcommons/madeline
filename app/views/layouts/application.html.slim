- content_for :template, yield(:title) || t(:loans)

- content_for :head
  = javascript_include_tag "application"
  = stylesheet_link_tag "application", :media => "all"
  = csrf_meta_tags

- content_for :content
  = render 'alerts'
  = yield

= render template: "layouts/wordpress-#{get_division_from_url}"
